import { COLORS } from '@/constants/colors';
import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const ProhibitationModalHeader = styled(Typography)`
  font-size: 1.2rem;
  text-align: center;
  color: ${COLORS.BLUE_600};
  font-weight: bold;
`;
